data:
  clean_path: datasets/clean.parquet
  path: datasets/BindingDB_All.tsv
  pki_threshold: 7.0
  split_ratios:
  - 0.7
  - 0.1
  - 0.2
smiles_encoders:
- type: fingerprint_mlp
  params:
    fp_type: ecfp
    fp_params:
      radius: 2
      fp_size: 1024
    hidden_dim: 512
    out_dim: 256
    dropout: 0.2
- type: rdkit_descriptors
  params:
    cache_path: datasets/rdkit_descriptors_full.pt
    hidden_dim: 512
    out_dim: 256
    dropout: 0.2
protein_encoders:
- type: cnn
  params:
    embed_dim: 256
    num_filters: 128
    kernel_sizes:
    - 3
    - 7
    - 15
    max_seq_len: 1000
fusion:
  type: mlp
  params:
    hidden_dims:
    - 256
    - 64
    dropout: 0.45
training:
  batch_size: 512
  learning_rate: 0.0003
  weight_decay: 1.0e-05
  epochs: 150
  patience: 10
  num_workers: 4
  device: auto
